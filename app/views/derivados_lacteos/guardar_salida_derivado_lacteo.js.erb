<% if @guardado_ok %>
 
  setTimeout( function(){ 
  
    $("#<%= params[:window_id]%>").modal('hide');
    $("#link-to-derivado-lacteo-detalle-<%= params[:derivado_lacteo_id]%>").trigger('click');
    $("#link-to-derivado-lacteo-detalle-<%= params[:derivado_lacteo_id]%>").trigger('click');

    swal("Derivados Lacteos","Salida Exitosa!", "success");

    }, 1000);

 
<% else %>

  swal("Derivados Lacteos", "Error al guardar la Salida: <%=@msg%>. Intente más tarde","error");

<% end %>