<% if @guardado_ok %>

 
setTimeout( function(){ 
  
    $("#<%= params[:window_id]%>").modal('hide');
    $("#link-to-ganado-detalle-<%= params[:ganado_id]%>").trigger('click');
    $("#link-to-ganado-detalle-<%= params[:ganado_id]%>").trigger('click');

    swal("Control Sanitario","Control de Alimentación agregado exitosamente!", "success");

    }, 1000);

 
<% else %>

    swal("Control Sanitario", "Error al guardar el Control Alimentación: <%=@msg%>. Intente más tarde","error");

<% end %>