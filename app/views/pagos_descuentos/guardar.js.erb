<% if @guardado_ok == true %>
 
  $("#<%= params[:window_id]%>").modal('hide');
  $("#form_buscar_registros_gastos_id").val('<%=@registro_gasto.id%>');
  $("#form_buscar_registros_gastos_fecha").val('');
  $("#form_buscar_registros_gastos_gasto").val('');
  $("#form_buscar_registros_gastos_monto").val('');
  $("#form_buscar_registros_gastos_observacion").val('');

  setTimeout( function(){ 

    swal("Registros Gastos","Gasto Registrado Exitosamente!", "success");

    }, 1000);

 	$("#form-buscar-registros-gastos").submit();

<% else %>
	
	$("<div class='col-sm-12' id='msg-guardar-registro-gasto' style='color:#000;padding-top:5px;font-size:10px;background: #f8dfdf;border-radius:5px;text-align:center;'><%= @msg %></div>").insertBefore("#form-agregar-registro-gasto");
 	swal("Registros Gastos", "Error al registrar el Gasto. Intente más tarde","error");

<% end %>