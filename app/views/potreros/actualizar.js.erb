<% if @potrero_ok.present? %>

   $("<div class='col-sm-12' id='msg-editar-potrero' style='color:#000;padding-top:5px;font-size:10px;background:#d6ffd6;border-radius:5px;text-align:center;'>Actualizado Exitosamente.</div>").insertBefore("#form-editar-potrero");
   
  $("#form_buscar_potreros_id").val('<%= @potrero.id %>');
  $("#form_buscar_potreros_codigo").val('');
  $("#form_buscar_potreros_monto").val('');
  $("#form_buscar_potreros_descripcion").val('');
  setTimeout(function(){ 

    $("#<%= params[:window_id]%>").modal('hide'); 
    $("#form-buscar-potreros").submit();
    swal("Guardado Exitosamente.");

  }, 3000);

<% else %>

  $("<div class='col-sm-12' id='msg-editar-potrero' style='color:#000;padding-top:5px;font-size:10px;background: #f8dfdf;border-radius:5px;text-align:center;'><%= j(@msg) %></div>").insertBefore("#form-editar-potrero");
   swal('<%= @msg.gsub('.','\n') %>');

<% end %>
