<% if reproducciones.present? %>

  <% reproducciones.each do |rep| %>

    <tr>

      <td><%= rep.reproduccion_id %></td>
      <td><%= rep.celo_ganado_rfid%></td>
      <td><%= rep.celo_ganado_nombre%></td>
      <td><%= rep.celo_ganado_rp%></td>
      <td ><%= rep.fecha_reproduccion.strftime('%d/%m/%Y') %></td>
      <td ><%= rep.fecha_concepcion.strftime('%d/%m/%Y')  %></td>
      <td ><%= rep.descripcion %></td>
      <td ><%= rep.hacienda %></td>
      <td ><%= rep.estado_reproduccion %></td>
      
      
      <td >
        
        <%= link_to_reproduccion_detalle(rep.reproduccion_id ) %>
       
        <%#if cel.estado_celo_id == PARAMETRO[:estado_celo_en_celo_activo] %>
          
          <%#= link_to_editar_celo(cel.celo_id) %>   
          <%#= link_to_cambiar_estado_a_en_reproduccion(cel.celo_id)%>
          <%#= link_to_cambiar_estado_a_celo_perdido(cel.celo_id)%>

          <%#= link_to icon_eliminar,"#", onclick: "eliminar_registro(#{cel.celo_id});return false;", class: "btn btn-danger btn-xs", id: "btn-eliminar-celo", title: 'Haga click para eliminar el celo'  %>

        <%#end%>

       </td>

   


    </tr>

  <% end %>

<tr id="tab-paginacion">

    <td colspan="12" style="text-align:center;"><div class="flickr_pagination"><%= ajax_will_paginate reproducciones, :params => params %></div></td>
</tr>

<tr>
    <td colspan="12">
      Cantidad de registros encontrados: <%= @total_encontrados %> 
    </td>
</tr>

 

<% else %>

  <tr>

    <td colspan="12"><p class='none-result'>No se han encontrado resultados.</p></td>

  </tr>


<% end %>

<script type="text/javascript">

  function eliminar_registro(reproduccion_id) {

        swal({
          title: "Confirmar",
          text: "Al confirmar se eliminará de la lista del módulo de Reproducciones y el Celo volverá al estado anterior, además el estado del ganado pasará al estado normal Activo (Celo Activo).",
          type: "warning",
          showCancelButton: true,
          confirmButtonClass: "btn-confirm",
          confirmButtonText: "Confirmar",
          cancelButtonText: "Cancelar",
          closeOnConfirm: true,
          closeOnCancel: true
        },
        function(isConfirm) {
          if (isConfirm) {

              $.ajax({
                url: "<%= url_for(reproducciones_eliminar_path) %>", 
                data: {id: reproduccion_id},
                dataType: 'script'
              });
            
          } else{

            return false;

          }

        });

    
    }

</script>
